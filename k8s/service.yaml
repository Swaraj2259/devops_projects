apiVersion: v1
kind: Service
metadata:
  name: globalchainx-service
  namespace: default
  labels:
    app: globalchainx
spec:
  type: NodePort
  selector:
    app: globalchainx
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000